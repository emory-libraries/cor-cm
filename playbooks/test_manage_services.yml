---
- hosts: curate-arch.library.emory.edu
  gather_facts: yes

  tasks:
    - ping:

    - include_role:
        name: emorylib_manage_aws_services
        apply:
          delegate_to: localhost
      vars:
        aws_services:
          - elb_target_group
     

#  roles:
#    - role: emorylib_manage_aws_services
#      vars:
#        aws_services:
#          - elb_target
#          - elb_application_lb
      