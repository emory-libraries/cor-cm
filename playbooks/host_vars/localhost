aws_region: us-east-1
ansible_become_pass: "{{ lookup('env', 'ANSIBLE_BECOME_PASS') }}"
ansible_connection: local
ansible_python_interpreter: '{{ ansible_playbook_python }}'

aws_services:
  - test
  - x

n2cw_cron_envs:
  - name: PATH
    value: '/home/{{ ansible_user }}/.bashrc'

n2cw_cron_jobs:
  - name: check_load
    namespace: nagiostest2
    disabled: yes
    minute: '*/5'
    options:
      - w 15,10,5
      - c 30,25,20

s3_custom_report:
  source:
    bucket: cor-s3-inventory-2
    key_prefix: fedora-cor-test-binaries/manifest
    date: 'latest'
  dest:
    bucket: cor-s3-inventory-3
    key_prefix: backup-fedora-cor-test-binaries/manifest
    date: 'latest'
s3_custom_report_manifest_name: manifest
s3_custom_report_download_location: /tmp