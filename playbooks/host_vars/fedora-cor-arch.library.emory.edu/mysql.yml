rds_host: dlp-tst-rdsmys-sz

fedora_mysql_db:
  name: '{{ vault_fedora.arch.db.name }}'
  user: '{{ vault_fedora.arch.db.user }}'
  password: '{{ vault_fedora.arch.db.pass }}'


myloader_download_path: /tmp # Path the restores are downloaded to
xmyloader_restores:
  - __user:
    __host:
    __database:
    __directory: /path/to/outputdir
    download_path: /tmp
  - __user:
    __host:
    s3:
      bucket:
      key_prefix:
      date: latest
  - s3:
      bucket:
      object: