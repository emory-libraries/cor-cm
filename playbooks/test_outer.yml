---
- hosts: localhost
  gather_facts: yes
  vars_files: temp_var.yml
  
  tasks:
    - include_tasks:
        file: test_inner.yml
      loop: '{{ list_var_names }}'
    
    - debug:
        var: title1