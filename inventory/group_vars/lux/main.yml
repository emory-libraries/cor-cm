project_name: ursus
idp_name: "prod"
ursus_dotenv:

ursus_linked_files:
  - content: "{{ lookup('template', 'dotenv_dict.j2') }}"
    dest: "{{ capistrano_base }}/{{ project_name }}/shared/.env.production"
    mode: 0644
