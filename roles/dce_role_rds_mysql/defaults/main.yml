---
# defaults file for dce_role_rds_mysql
# Needed for role logic
rails_db_name: "placeholderdb"
rails_db_user: "placeholderuser"
rails_db_pass: "placepass"
rails_db_user_host: "localhost"

fedora_db_name: "placeholderdb"
fedora_db_user: "placeholderuser"
fedora_db_password: "placepass"
fedora_db_user_host: "localhost"

rds_endpoint: "{{ hostvars['localhost']['rds_endpoint'] }}"
rds_master_username: "{{ hostvars['localhost']['rds_master_username'] }}"
rds_master_password: "{{ hostvars['localhost']['rds_master_password'] }}"

db_state: present
db_user_state: present
