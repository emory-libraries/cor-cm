- name: Get file from s3 bucket
  aws_s3:
    bucket: "{{ aws_s3_bucket_name }}"
    dest: "{{ aws_s3_dest }}"
    object: "{{ aws_s3_object }}"
    region: "{{ aws_s3_region | d (aws_region) }}"
    version: "{{ aws_s3_version | d (omit) }}"
    mode: "{{ aws_s3_mode | d ('get') }}"
