---
- name: Set userdata for EC2 (No EFS)
  set_fact:
    ec2_user_data: "{{ lookup('template', 'user_data.j2') }}"
  when: not ec2_efs | d (False)

- name: Set userdata for EC2 (EFS)
  set_fact:
    ec2_user_data: "{{ lookup('template', 'user_data_efs_mnt.j2') }}"
  when: ec2_efs | d (False)
